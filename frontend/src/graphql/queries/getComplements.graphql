query GetComplements($categoryInput: String) {
    getComplements(categoryInput: $categoryInput) {
        id
        name
        description
        price
        category
        sizePiecesGrams
        image
        ingredients {
            name
            quantity
        }
    }
}